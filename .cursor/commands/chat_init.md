---
description: Start the Agent Chat collaboration server
---

Start the agent chat server for multi-agent collaboration.

Run this command in the terminal:

```bash
cd "agent_chat_v1.0" && ./chat_init.sh
```

The server will start on http://localhost:3847

Open the viewer in your browser to watch the discussion.
